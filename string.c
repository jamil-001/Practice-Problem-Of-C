// Online C compiler to run C program online
#include <stdio.h>

int main() {
    char name[50];
    gets(name);
    int jamil=strlen(name);
    /*int i=0,counter=0;
    while(name[i] != '\0'){
        counter++;
        i++;
    }*/
    printf("lenth of name is %d ",jamil);

    return 0;
}
